/*#include "lib.h"
#include <assert.h>
#include <math.h>

void test_multiply() {
    double a[2][3] = {{1, 2, 3}, {4, 5, 6}};
    double b[3][2] = {{7, 8}, {9, 10}, {11, 12}};
    double c[2][2];

    multiply_matrices(2, 3, 2, a, b, c);

    assert(fabs(c[0][0] - 58) < 1e-6);
    assert(fabs(c[0][1] - 64) < 1e-6);
    assert(fabs(c[1][0] - 139) < 1e-6);
    assert(fabs(c[1][1] - 154) < 1e-6);
}

int main(void) {
    test_multiply();
    return 0;
}
*/
