/*#include "../src/module.h"
#include <assert.h>
#include <stdio.h>


void test_add_element() {
    MyArray arr;
    myarray_init(&arr);
    myarray_add(&arr, 42);
    assert(arr.size == 1);
    assert(arr.data[0] == 42);
    myarray_free(&arr);
}


void test_find_element() {
    MyArray arr;
    myarray_init(&arr);
    myarray_add(&arr, 10);
    myarray_add(&arr, 20);
    int idx = myarray_find(&arr, 20);
    assert(idx == 1);
    myarray_free(&arr);
}

int main() {
    test_add_element();
    test_find_element();
    printf("All tests passed!\n");
    return 0;
}
*/
